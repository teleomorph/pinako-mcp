{
  "name": "pinako-mcp",
  "version": "1.1.0",
  "description": "MCP server bridge for Pinako browser extension",
  "type": "module",
  "scripts": {
    "install-host": "node install.js",
    "build": "node setup/build.js",
    "build:all": "node setup/build.js --all",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.143",
    "esbuild": "^0.25.0",
    "pkg": "^5.8.1",
    "vitest": "^2.1.9"
  }
}
